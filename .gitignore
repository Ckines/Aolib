# Build artifacts
*.o
*.d

# Compiled core binaries (never committed)
aolib_libretro.dll
aolib_libretro.so
aolib_libretro.dylib

# dist output (binary + .info copies, generated by 'make dist*')
dist/
dist-windows/
dist-linux/

# OS/editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.swp

# Internal-only: test suite, the pre-release checklist and notes on local
# patches to vendored deps. Kept in the private working tree, not published.
tests/
verificar_antes_de_subir.sh
deps/patches/
